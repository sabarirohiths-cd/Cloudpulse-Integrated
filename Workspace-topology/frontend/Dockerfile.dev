FROM node:20-alpine

WORKDIR /app

# Copy package files and install dependencies
COPY package.json package-lock.json* ./
RUN npm install

# Start the dev server on 5174
CMD ["npm", "start", "--", "--host", "0.0.0.0", "--port", "5174"]
